---
tool: show_table
type: registry:block
---

# show_table

Use for small static snapshots where display-ready rows are already available.

Avoid `show_table` when the user needs browsing, filtering, sorting, pagination, details, row actions, or live data access. Use `show_data_browser` for those cases.
